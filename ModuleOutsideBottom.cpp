#include "ModuleOutsideBottom.h"

/*
void DataStorage::onDataReceive(const uint8_t* macAddress, const uint8_t* incomingData, int dataLength) {
	//DataStorage ds;
	memcpy(&currentvalue, incomingData, sizeof(currentvalue));
	Serial.println("Data received: " + String(dataLength));
	currentvalue[0] = float(currentvalue[0]);
	currentvalue[1] = float(currentvalue[1]);
	currentvalue[2] = float(currentvalue[2]);
	Serial.println("----------------------------");
}
*/